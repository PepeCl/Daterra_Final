package com.example.daterra.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta principal Daterra
val DaterraPrimary= Color(0xFF6C9B47)      // Color Primario (Botones, encabezados)
val DaterraSecundary = Color(0xFF039BE5)      // Color Secundario (Enlaces, iconos)
val DaterraAccent = Color(0xFFFFCA28)     // Color Acento (Destacados)

// Colores neutrales
val DaterraBackground = Color(0xFFFFFFFF) // Fondo Ligero
val DaterraText = Color(0xFF263238)       // Texto Principal
val DaterraBorder = Color(0xFFB2DFDB)     // Líneas y Bordes